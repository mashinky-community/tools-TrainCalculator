<Window x:Class="MashinkyCalculator.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MashinkyCalculator"
        mc:Ignorable="d"
        Title="HelpWindow" Height="360" Width="450" Background ="#38372e" WindowStartupLocation="CenterScreen" FontSize="14" Foreground="#f1edca">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Button x:Name="CloseButton" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Content="Close" Foreground="#f1edca" Background="#25261e" Click="CloseButton_Click"></Button>
        <TextBlock Margin="20,20" TextWrapping="Wrap" Grid.Row="0">
            To calculate trains you have to fill train length and select First cargo. <LineBreak/>
            Speed parameter filters engines within +/- 10 km/h. You can change that range in Settings - Speed tolerance. <LineBreak/>
            Default speed unit is kilometers per hour. You can change it to miles per hour in Settings. <LineBreak/>
            To input decimal cargo ratios multiple both values by 10 :) . <LineBreak/>
            When you set Epoch filter to "0", calculator will use engines/wagons from all eras. Might cause lag while calculating. <LineBreak/>
            Only mods activated in Mashinky Extension menu will be loaded. <LineBreak/>

        </TextBlock>
    </Grid>
</Window>
